<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:CellularCompany.ViewModels"
             xmlns:model="clr-namespace:CellularCompany.Models"
             x:Class="CellularCompany.Views.ContractsPage"
             x:DataType="vm:ContractsViewModel"
             Title="Contracts">

    <Grid RowDefinitions="Auto,*" Padding="16">
        <Button Text="Add Contract"
                Command="{Binding AddContractCommand}"
                HorizontalOptions="End"
                Margin="0,0,0,16"/>

        <RefreshView Grid.Row="1"
                     IsRefreshing="{Binding IsLoading}"
                     Command="{Binding LoadContractsCommand}">
            <CollectionView ItemsSource="{Binding Contracts}">
                <CollectionView.EmptyView>
                    <Label Text="No contracts found"
                           HorizontalOptions="Center"
                           VerticalOptions="Center"
                           TextColor="{StaticResource Gray600}"/>
                </CollectionView.EmptyView>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="model:Contract">
                        <Frame Margin="0,0,0,12" Padding="16">
                            <Frame.GestureRecognizers>
                                <TapGestureRecognizer
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type vm:ContractsViewModel}}, Path=EditContractCommand}"
                                    CommandParameter="{Binding .}"/>
                            </Frame.GestureRecognizers>
                            <Grid ColumnDefinitions="*,Auto">
                                <VerticalStackLayout Spacing="4">
                                    <Label Text="{Binding ContractNumber}"
                                           FontSize="16"
                                           FontFamily="OpenSansSemibold"/>
                                    <Label Text="{Binding StartDate, StringFormat='Start: {0:d}'}"
                                           TextColor="{StaticResource Gray600}"/>
                                    <Label Text="{Binding EndDate, StringFormat='End: {0:d}'}"
                                           TextColor="{StaticResource Gray600}"/>
                                    <Label Text="{Binding Status, StringFormat='Status: {0}'}"
                                           TextColor="{StaticResource Secondary}"
                                           FontSize="12"/>
                                </VerticalStackLayout>
                                <Button Grid.Column="1"
                                        Text="Delete"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type vm:ContractsViewModel}}, Path=DeleteContractCommand}"
                                        CommandParameter="{Binding .}"
                                        BackgroundColor="{StaticResource Secondary}"
                                        VerticalOptions="Center"/>
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>
    </Grid>

</ContentPage>
